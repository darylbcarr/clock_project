❯ The names that Alexa sees: First Device, First Light, Second Light. How do I interpret these names and devices. I see First Light and Second Light are the 
 lights. What exactly is First Device?                                                                                                                       

● In Matter, a single physical device (the clock) is a Node containing multiple Endpoints:

  ┌───────────┬───────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐    
  │  Alexa    │   Matter entity   │                                                     What it is                                                      │
  │   name    │                   │                                                                                                                     │    
  ├───────────┼───────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ First     │ Endpoint 0 — Root │ The device itself. Contains Basic Information cluster (vendor, model, firmware version). Alexa surfaces it as a     │
  │ Device    │  Node             │ controllable entity even though it's really just metadata.                                                          │
  ├───────────┼───────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ First     │ Endpoint 1        │ Ring LED strip (LedManager STRIP_1, GPIO1)                                                                          │
  │ Light     │                   │                                                                                                                     │
  ├───────────┼───────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Second    │ Endpoint 2        │ Base LED strip (LedManager STRIP_2, GPIO2)                                                                          │
  │ Light     │                   │                                                                                                                     │
  └───────────┴───────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

  "First Device" isn't really useful to control — it's the Matter node descriptor, not an actual light. Alexa happens to expose it because the root endpoint
  has a device type cluster that Alexa treats as a thing. You can't do much useful with it.

  The names ("First", "Second") are just Alexa's auto-generated defaults for unnamed devices. You can rename all three in the Alexa app to whatever you want
  (e.g., "Clock Ring", "Clock Base") and Alexa will use those going forward.

  If you want the device to advertise a meaningful name at the hardware level (so it shows up with that name on first discovery), you'd set node_label in the
   Basic Information cluster config in matter_bridge.cpp:

  esp_matter::node::config_t node_cfg = {};
  strncpy(node_cfg.basic_information.node_label, "Analog Clock", sizeof(node_cfg.basic_information.node_label) - 1);

  That would change "First Device" → "Analog Clock" on fresh pairing. The light endpoint names would still be Alexa defaults unless you also set their
  node_label fields.

--------------------------------
                                                                                                                                                             
● Clean build. Flash it:                                                                                                                                     
                                                                                                                                                             
  ./build.sh -p /dev/ttyUSB0 flash monitor                                                                                                                   
                                                                                                                                                             
  After flashing you'll need to remove and re-add in Alexa again (capability change requires re-discovery). The new FeatureMap will be 0x19 (HS+XY+CT), and
  Alexa should now:                                                                                                                                          
                                                                                     
  - Show a color picker instead of just CT slider
  - Send MoveToHueAndSaturation when you say "set to green" → you'll see W : Attribute 0x00000001 (CurrentSaturation) and 0x00000000 (CurrentHue) in the log
  instead of the CT attribute

  The root issue was that Alexa maps color names exclusively to MoveToHueAndSaturation commands — XY mode alone isn't enough. Without the HS feature bit in
  FeatureMap/ColorCapabilities, Alexa treated the device as a white-spectrum-only light and converted "green" to a cool color temperature.

✻ Cogitated for 5m 16s · 1 background task still running (↓ to manage)

------------------------------

❯ Alexa discovery is failing. The device still has the connection (fabric)? Does the device need to be 'reset'?                                              
                                                                                                                                                             
● Yes — the device has an existing fabric, so BLE commissioning isn't open. Alexa has nothing to discover. You need to clear the Matter fabric from NVS.     
                                                                                                                                                             
  Check the partition addresses first:                                                                                                                       
                                                                                     
● Read 1 file (ctrl+o to expand)                                                                                                                             
                                                                                                                                                             
● Good. The nvs partition holds the CHIP fabric/KVS data. The fctry partition holds the discriminator — we want to keep that. Erase only nvs, then flash the 
  new firmware:                                                                                                                                              
                                                                                                                                                             
  # 1. Erase just the nvs partition (clears Matter fabric, preserves fctry discriminator)                                                                    
  python -m parttool -p /dev/ttyUSB0 erase_partition --partition-name nvs                                                                                    
                                                                                                                                                             
  # 2. Flash new firmware                                                                                                                                    
  ./build.sh -p /dev/ttyUSB0 flash monitor                                           

  After boot, the device has no fabric so BLE commissioning opens automatically. Then:
  1. In Alexa → Add Device → Other (or matter device) → scan/enter the code
  2. The boot log will print the PIN and manual pairing code as before

  If parttool isn't on your PATH, the equivalent esptool command (nvs starts at 0x9000, size 0xC000):
  python -m esptool -p /dev/ttyUSB0 erase_region 0x9000 0xC000

✻ Brewed for 40s · 1 background task still running (↓ to manage)
                                                                                          
-----------------------------

● New workflow:

  # After changing code:
  ./build.sh build

  # To flash (seconds, not minutes):
  ./build.sh -p /dev/ttyUSB0 flash-fast monitor

  flash-fast monitor opens the idf.py monitor immediately after flashing, so you get the serial output without any extra steps.

------------------------------

● Flash and run two tests back to back, watching the monitor:

  Test A — QR scan:
  1. Boot → wait for commissioning info
  2. Open Alexa → Add Device → Matter → scan the website QR
  3. Wait for Alexa to time out
  4. Note: did >>> BLE connection established appear in the log?

  Test B — Manual code:
  1. Type matter-pair at prompt to reopen fast advertising
  2. Open Alexa → Add Device → Matter → enter 01693312333 manually
  3. Note: did >>> BLE connection established appear?

  Interpretation:
  - QR = no BLE connection event → Alexa rejected before connecting, almost certainly the VID=0/PID=0 DCL check. The fix is to make the website skip VID/PID
  in the QR (not possible — they're required fields) OR accept that Alexa's QR path requires DCL registration and the manual code is the correct workflow for
   this device.
  - QR = BLE connection established but then fails → something in PASE or commissioning differs between the two paths, which would be very unusual.

  My money is on "no BLE event with QR" — which would confirm the manual code is simply the right tool for a dev device and we should make that prominent on
  the website.

✻ Sautéed for 26m 8s · 2 background tasks still running (↓ to manage)

● Background command "bash build.sh build 2>&1 | tail -8" completed (exit code 0)

● Background command "./build.sh build 2>&1 | tail -8" completed (exit code 0)

● Background command "./build.sh build 2>&1 | grep -E "error:|Build complete|clock_project.bin" | tail -5" completed (exit code 0)

● Background command "./build.sh build 2>&1 | tail -8" completed (exit code 0)

● All stale background build notifications — the foreground build already confirmed clean and committed. Nothing to action.

  Go ahead and flash and run the two tests when you're ready. The key thing to watch for is whether >>> BLE connection established appears in the log during
  the QR scan attempt.

-------------------------------------------------

New button controls in text input:                                                                                             
  
  ┌─────────────────────────────────────┬──────────────────────────────────────────────────────┐                                 
  │               Gesture               │                        Action                        │                               
  ├─────────────────────────────────────┼──────────────────────────────────────────────────────┤                                 
  │ Tap A                               │ Navigate backward one step                           │                               
  ├─────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ Hold A (accelerates at 250ms)       │ Keep navigating backward                             │                                 
  ├─────────────────────────────────────┼──────────────────────────────────────────────────────┤                                 
  │ Hold A to 800ms                     │ Append current character (fires at 800ms, stops nav) │                                 
  ├─────────────────────────────────────┼──────────────────────────────────────────────────────┤                                 
  │ Tap B                               │ Navigate forward one step                            │                               
  ├─────────────────────────────────────┼──────────────────────────────────────────────────────┤                                 
  │ Hold B (accelerates at 250ms)       │ Keep navigating forward                              │                               
  ├─────────────────────────────────────┼──────────────────────────────────────────────────────┤                                 
  │ Hold B to 800ms                     │ Backspace (fires at 800ms, stops nav)                │                               
  ├─────────────────────────────────────┼──────────────────────────────────────────────────────┤                                 
  │ Press A+B briefly (release < 800ms) │ Confirm / accept                                     │
  ├─────────────────────────────────────┼──────────────────────────────────────────────────────┤                                 
  │ Hold A+B to 800ms                   │ Cancel (returns to previous screen)                  │                               
  ├─────────────────────────────────────┼──────────────────────────────────────────────────────┤                                 
  │ Encoder rotate                      │ Navigate (all devices)                               │                               
  ├─────────────────────────────────────┼──────────────────────────────────────────────────────┤                                 
  │ Encoder short press                 │ Append (or confirm if on >)                          │                               
  ├─────────────────────────────────────┼──────────────────────────────────────────────────────┤                                 
  │ Encoder long press                  │ Backspace                                            │                               
  └─────────────────────────────────────┴──────────────────────────────────────────────────────┘                                 
                                                                                                                               
  Scrolling speed: initial acceleration delay cut from 400ms → 250ms, repeat rate from 100ms → 65ms (≈1.5× faster throughout).   
  From the initial press, after 250ms the cursor starts running at ~15 steps/second.                                           
                                                                                                                                 
  The hint screen now shows the button controls clearly for 1.8s before the grid appears.                                        

------------------------

 Boot-time failure prompt (show_wifi_failed_prompt):                                                                            
                                                                                                                                 
  ┌─────────────────────────────┬─────────────┐                                                                                  
  │            Input            │   Action    │                                                                                  
  ├─────────────────────────────┼─────────────┤
  │ Hold A+B 2s                 │ Reconfigure │                                                                                  
  ├─────────────────────────────┼─────────────┤
  │ Hold encoder button 5s      │ Reconfigure │
  ├─────────────────────────────┼─────────────┤
  │ Release encoder button < 5s │ Keep trying │                                                                                  
  ├─────────────────────────────┼─────────────┤
  │ Single A or B press         │ Keep trying │                                                                                  
  ├─────────────────────────────┼─────────────┤
  │ 30s timeout                 │ Keep trying │                                                                                  
  └─────────────────────────────┴─────────────┘
                                                                                                                                 
  Display shows: "Reconfigure: / A+B 2s/enc 5s" and "Keep trying: / btn/enc/30s"
                                                                                                                                 
  Panic hold (encoder_task, from anywhere including blanked display):
                                                                                                                                 
  ┌─────────────────────┬───────────┬──────────────────────┐
  │        Input        │ Threshold │        Action        │                                                                     
  ├─────────────────────┼───────────┼──────────────────────┤
  │ Hold A+B            │ 3s        │ Warning screen       │                                                                     
  ├─────────────────────┼───────────┼──────────────────────┤
  │ Hold A+B            │ 5s        │ Clear WiFi + restart │
  ├─────────────────────┼───────────┼──────────────────────┤                                                                     
  │ Hold encoder button │ 3s        │ Warning screen       │
  ├─────────────────────┼───────────┼──────────────────────┤                                                                     
  │ Hold encoder button │ 5s        │ Clear WiFi + restart │                                                                     
  └─────────────────────┴───────────┴──────────────────────┘
                                                                                                                                 
  Note on encoder panic: the normal long press (800ms) fires back first, then the panic counter continues accumulating. So the
  user will navigate up one menu level before seeing the warning at 3s — same behavior as the A+B path where both_fall fires back
   on press. 

   ---------------------

  ---
  OLED Menu: System → Update

  ┌────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │    Item    │                                                              Behaviour                                                              │
  ├────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Check Now  │ Shows current version, fetches latest from GitHub (up to 20s), displays result. If update available: prompts A/Select: Install or   │
  │            │ B: Cancel                                                                                                                           │
  ├────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Auto       │ Toggles on/off with a brief confirmation screen; persisted to NVS                                                                   │
  │ Update     │                                                                                                                                     │
  └────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

  ---
  Web UI: Config → Firmware Update

  ┌───────────────────┬──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │      Control      │                                                         What it does                                                         │
  ├───────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Running / Latest  │ Version fields updated every second via WebSocket                                                                            │
  ├───────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Check for Updates │ Posts {action:"check"} to /api/ota — result appears in a few seconds                                                         │
  ├───────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Update Now        │ Appears (highlighted) when an update is available; browser confirm dialog then posts {action:"update"} — device restarts     │
  │                   │ when done                                                                                                                    │
  ├───────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Auto Update       │ Enabled/Disabled buttons — saves to NVS immediately                                                                          │
  │ toggle            │                                                                                                                              │
  └───────────────────┴──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

  The status line shows ⚠ Update available!, ✓ Up to date, or Checking... automatically.

----------------------------------

 Before the devices update, there's one step you likely missed — bumping the version number in CMakeLists.txt. Without that, the binary the Action builds still reports
  1.0.0, which would match the running version and devices wouldn't update (or worse, update in a loop).
                                                                                                                                                                           
  The correct release process:
                                                                                                                                                                           
  1. Make your code changes                                                                                                                                                
  2. Bump the version in CMakeLists.txt:
  set(PROJECT_VER "1.1.0")                                                                                                                                                 
  3. Commit and tag:                                                                                                                                                       
  git add CMakeLists.txt                                                                                                                                                   
  git commit -m "Bump to v1.1.0"                                                                                                                                           
  git tag v1.1.0 && git push origin main v1.1.0                                                                                                                            
                                                                                                                                                                           
  Since you already pushed the v1.1.0 tag without bumping, the Action probably built a binary that still says 1.0.0. Check the Action status at:                           
                                                                                                                                                                           
  ▎ https://github.com/darylbcarr/clock_project/actions                                                                                                                    
                                                                                                                                                                         
  If it ran and created a release, delete the tag and release, bump the version, and re-tag as v1.1.1:                                                                     
                                                                                                                                                                         
  # Delete the bad tag                                                                                                                                                     
  git tag -d v1.1.0                                                                                                                                                      
  git push origin :refs/tags/v1.1.0                                                                                                                                        
                                                                                                                                                                           
  # Bump version, re-tag                                                                                                                                                   
  # (edit CMakeLists.txt first: set PROJECT_VER "1.1.1")                                                                                                                   
  git add CMakeLists.txt && git commit -m "Bump to v1.1.1"                                                                                                                 
  git tag v1.1.1 && git push origin main v1.1.1                                                                                                                            
                                                                                                                                                                           
  Once the Action completes successfully, version.json on GitHub will say 1.1.1, devices will detect the mismatch with their running 1.0.0, and update automatically within
   24h (or immediately via Check for Updates).                                                                                                                           
                                                                                                                                                                           
----------------------------------

The GitHub Action (.github/workflows/release.yml) handles everything automatically:

  1. Trigger: When you push a v* tag, GitHub detects it and starts the Action                                                                                              
  2. Build: It runs idf.py build inside an espressif/esp-idf-ci-action Docker container (ESP-IDF v5.4.1, target esp32s3), producing build/clock_project.bin
  3. Release: It creates a GitHub Release at github.com/darylbcarr/clock_project/releases/tag/v1.1.2 and uploads the .bin as a downloadable asset                          
  4. version.json: It updates version.json on main with the new version + direct download URL, so your devices find it on their next check                                 
                                                                                                                                                                           
  Since you've already bumped PROJECT_VER to "1.1.2", you're ready to tag and release:                                                                                     
                                                                                                                                                                           
  git add CMakeLists.txt                                                                                                                                                   
  git commit -m "Bump version to 1.1.2"                                                                                                                                    
  git tag v1.1.2
  git push origin main v1.1.2                                                                                                                                              
                                           
  Then watch the Action run at https://github.com/darylbcarr/clock_project/actions. Once it finishes (~5-10 min), the .bin will be at                                      
  https://github.com/darylbcarr/clock_project/releases/tag/v1.1.2 and version.json will be updated to point to it.
                                                                                                                                                                           
  If you tagged v1.1.0 without a proper build action in place yet, you may want to delete it first to avoid confusion:                                                     
  git tag -d v1.1.0
  git push origin :refs/tags/v1.1.0                                                                                                                                        

-----------------------------------

All changes done. Here's a summary:

  No restart required for hostname changes — mdns_hostname_set() works live, announces the new name and withdraws the old one via mDNS goodbye packets.

  What was changed:

  ┌───────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │           File            │                                                                 Change                                                                  │
  ├───────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ config_store.h            │ Added mdns_hostname[32] to NetCfg                                                                                                       │
  ├───────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ config_store.cpp          │ Persist/load "net_mdns" NVS key                                                                                                         │
  ├───────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ networking.h              │ Added mdns_hostname to NetStatus; set_mdns_hostname_hint() (pre-boot), set_mdns_hostname() (live), start_mdns() (private)               │
  ├───────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ networking.cpp            │ start_mdns() called from on_got_ip() — derives clock_XXXX from last 2 MAC bytes if no hint saved; set_mdns_hostname() calls             │
  │                           │ mdns_hostname_set() live                                                                                                                │
  ├───────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ networking/CMakeLists.txt │ Added mdns to REQUIRES                                                                                                                  │
  ├───────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ webserver.cpp             │ on_api_cfg handles mdns_hostname (live update + persist); build_status_json adds mdns_hostname field                                    │
  ├───────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ web_ui.h                  │ "Device Hostname" card added under Config; applyData populates input and display; saveMdnsHostname() JS                                 │
  ├───────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ main.cpp                  │ Passes netCfg.mdns_hostname to set_mdns_hostname_hint() on both boot paths                                                              │
  └───────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

  On first boot the device will be at clock_XXXX.local (last 4 hex of MAC). The web Config tab will show and let you change it.

------------------------------------------------

 Now I have the full picture. Three things to address:

  ---
  1. Why First-Time Setup recurs (root cause)

  is_commissioned() calls FabricTable.FabricCount() > 0. In the log, commissioning failed because our mDNS conflict killed the BLE link before Alexa's
  CommissioningComplete arrived. CHIP's fail-safe timer (armed at t=44832ms for 300s) then expired and rolled back the fabric. On next boot, FabricCount == 0 → First-Time
  Setup. The mDNS fix we just made should prevent this.

  ---
  2. Device state cheat sheet

  ┌───────────────────────────────┬──────────────────────────────────┐
  │           NVS state           │            What boots            │
  ├───────────────────────────────┼──────────────────────────────────┤
  │ netCfg.ssid set               │ Connects directly, skips setup   │
  ├───────────────────────────────┼──────────────────────────────────┤
  │ ssid empty + FabricCount > 0  │ Skips setup, Matter manages WiFi │
  ├───────────────────────────────┼──────────────────────────────────┤
  │ ssid empty + FabricCount == 0 │ First-Time Setup screen          │
  ├───────────────────────────────┼──────────────────────────────────┤
  │ Fully erased flash            │ First-Time Setup (defaults)      │
  └───────────────────────────────┴──────────────────────────────────┘

  You can check from the UART console at any time:
  clock> status        # shows WiFi/SNTP state
  clock> check-ota     # shows firmware version

  ---
  3. Full erase + fresh flash

  # Erase everything (NVS, OTA state, partition table, app)
  idf.py -p /dev/ttyUSB0 erase-flash

  # Then flash fresh (builds + flashes all partitions)
  ./build.sh -p /dev/ttyUSB0 flash monitor

  The partition table comment even documents this: "Requires full reflash: idf.py -p /dev/ttyUSB0 erase-flash flash".

  ---
  4. Factory image for new hardware

  After a successful build, create a single merged binary that can be written to any blank ESP32-S3:

  # Create merged factory image (combines bootloader + partitions + app)
  python $IDF_PATH/components/esptool_py/esptool/esptool.py \
    --chip esp32s3 merge_bin \
    --output factory_image.bin \
    @build/flash_args

  build/flash_args is generated by idf.py build and contains the exact offsets for every binary. The merged image starts at 0x0 and can be flashed to new hardware:

  # Flash factory image to new blank board
  esptool.py --chip esp32s3 -p /dev/ttyUSB0 --baud 921600 \
    write_flash 0x0 factory_image.bin

  One caveat — fctry partition (Matter attestation): Your partition table has a fctry NVS partition that stores the Matter Device Attestation Certificate (DAC). For
  test/dev this uses Espressif's test DAC and is the same on every device, so the merged image works as-is. For a commercial product each device needs a unique DAC flashed
   at manufacture time — that's a separate provisioning step.

----------------------------------------

The "TEST_VENDOR" name comes from the development Device Attestation Certificate (DAC) embedded in esp_matter. There are two ways to address this:                       
                                                                                                                                                    
  Short term: Change via Kconfig (changes what the app reports, but DAC still says TEST_VENDOR)                                                                            
                                                                                                                                                                           
  You can set CONFIG_DEVICE_VENDOR_NAME and CONFIG_DEVICE_PRODUCT_NAME in sdkconfig.defaults:                                                                              
                                                                                                                                                                           
  CONFIG_DEVICE_VENDOR_NAME="My Clock"                                                                                                                                   
  CONFIG_DEVICE_PRODUCT_NAME="Analog Clock"                                                                                                                                
                                                                                                                                                                           
  However, what Alexa displays as the device name is typically the NodeLabel or ProductName from the Basic Information cluster — and crucially, the name shown during      
  commissioning comes from the DAC, which Alexa uses to verify the device's identity. The DAC's VendorName field is what shows as "TEST_VENDOR".                           
                                                                                                                                                                           
  The real answer: Yes and no                                                                                                                                              
                                                                                                                                                                           
  - The label/friendly name in Alexa (the device name you see after commissioning, like "Clock" or "Test Device") — this can be renamed in the Alexa app after
  commissioning. Just long-press or tap the device in the Alexa app → Settings → Device name.                                                                              
  - "TEST_VENDOR" during commissioning — this comes from the test DAC (VendorID 0xFFF1 is the reserved test vendor). It only appears during the pairing flow itself, not as
   the permanent device name.                                                                                                                                              
  - Permanently changing the vendor name — requires production DACs from the CSA/Matter certification process. You'd get real VendorID, real VendorName, and proper        
  ProductID. This is a production certification cost/process.                                                                                                      
                                                                                                                                                                           
  For a personal/dev device, the practical path is: let it say "TEST_VENDOR" during commissioning, then rename the device in the Alexa app. The commissioning warning is
  just because it's an uncertified/test device — Alexa will still add it.                                                                                                  
                                           
----------------------------------------------------------

How should updates/commits/versioning be related?                                                                                                                        
                                                    
● Searched for 2 patterns, read 1 file (ctrl+o to expand)                                                                                                                  
                                                                                                                                                                         
● Now I have the full picture. Here's how it all fits together and the recommended workflow:                                                                               
                                                                                            
  ---                                                                                                                                                                      
  How Versioning, Commits, and Releases Relate                                                                                                                             
                                                                                                                                                                           
  The Version Number Lives in One Place                                                                                                                                    
                                           
  CMakeLists.txt → set(PROJECT_VER "1.1.2")                                                                                                                                
                                                                                                                                                                           
  ESP-IDF bakes this into the firmware binary as app_desc.version. OtaManager::running_version() reads it at runtime via esp_app_get_description(). The OTA checker        
  compares it against version.json on GitHub to decide if an update is available.                                                                                          
                                                                                 
  ---                                                                                                                                                                      
  The Three-Step Workflow                  
                         
  code changes  →  commit(s)  →  bump version + tag  →  push tag  →  CI builds + releases                                                                                  
                                                                                         
  1. Regular development commits — commit as often as you like, no version change needed. These are just work in progress on main.                                         
                                                                                                                                                                           
  2. When ready to release — do this in a single commit:                                                                                                                   
  # Edit CMakeLists.txt: bump PROJECT_VER "1.1.2" → "1.1.3"                                                                                                                
  git add CMakeLists.txt                                   
  git commit -m "Bump to v1.1.3"                                                                                                                                           
  git tag v1.1.3                                                                                                                                                           
  git push origin main v1.1.3                                                                                                                                              
                                                                                                                                                                           
  Pushing the tag triggers the GitHub Actions workflow, which builds, creates a GitHub Release with clock_project.bin, and updates version.json on main. Devices polling   
  for OTA will see the new version within their next check interval.                                                                                                       
                                                                                                                                                                           
  3. The tag is the source of truth — the tag name must match PROJECT_VER. If they differ, the OTA check will never trigger (running version won't match version.json).    
                                                                                                                                                                           
  ---                                      
  Versioning Convention (Semantic Versioning)                                                                                                                              
                                             
  ┌──────────────┬───────────────┬────────────────────────────────────────────────────────────────────────────────────┐                                                    
  │ Change type  │    Example    │                                    When to use                                     │
  ├──────────────┼───────────────┼────────────────────────────────────────────────────────────────────────────────────┤                                                    
  │ Patch x.x.+1 │ 1.1.2 → 1.1.3 │ Bug fixes, minor tweaks                                                            │
  ├──────────────┼───────────────┼────────────────────────────────────────────────────────────────────────────────────┤                                                    
  │ Minor x.+1.0 │ 1.1.3 → 1.2.0 │ New features (mDNS, new menu items)                                                │
  ├──────────────┼───────────────┼────────────────────────────────────────────────────────────────────────────────────┤                                                    
  │ Major +1.0.0 │ 1.2.0 → 2.0.0 │ Breaking changes (partition layout change, NVS schema change requiring full erase) │                                                    
  └──────────────┴───────────────┴────────────────────────────────────────────────────────────────────────────────────┘                                                    
                                                                                                                                                                           
  Partition layout changes (like the one adding fctry) require a full erase and manual flash — OTA can't handle those. That warrants a major bump and a release note
  warning users.                                                                                                                                                           
                                           
  ---                                                                                                                                                                      
  Current State                            
               
  Your working tree has several uncommitted changes across multiple sessions. A clean approach:                                                                            
                                                                                               
  # Commit all the accumulated work                                                                                                                                        
  git add components/ main/                
  git commit -m "Add mDNS, fix Matter reboot, improve Net Status display"                                                                                                  
                                                                                                                                                                           
  # Then bump version and release                                                                                                                                          
  # Edit CMakeLists.txt: 1.1.2 → 1.2.0  (new features warrant minor bump)                                                                                                  
  git add CMakeLists.txt                                                                                                                                                   
  git commit -m "Bump to v1.2.0"                                                                                                                                           
  git tag v1.2.0                
  git push origin main v1.2.0                                                                                                                                              
